# Client Environment Variables for Docker
# Copy this to .env when running with Docker Compose

# API URL - Points to the server service
# In Docker Compose, use the service name
VITE_API_URL=http://localhost:5000/api